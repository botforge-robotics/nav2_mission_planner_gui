# UI (host network → http://localhost:8080)
WEB_PORT=8080

# Optional: force LAN scan subnet, e.g. 192.168.1.0/24
# Leave empty to auto-detect from API container interfaces
SCAN_CIDR=

# SQLite claim + settings live in Docker volume mission_planner_data
